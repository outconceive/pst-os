[package]
name = "sel4-sys"
version = "0.1.0"
edition = "2021"

# No dependencies — this is a raw FFI binding crate.
# bindings.rs is generated by bindgen from the seL4 C headers.
# Run: bash tools/build/gen-bindings.sh to regenerate.

[dependencies]

[features]
default = []
